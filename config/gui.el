; File loaded only when on graphical emacs

; Disabling toolbar, scrollbar and menubar
(tool-bar-mode -1)
(toggle-scroll-bar -1)
(menu-bar-mode -1)

; Disabling fringe (that little column on the left)
(set-fringe-mode 0)


